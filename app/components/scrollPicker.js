import React from "react";
import { View, Text } from "react-native"

import { default as colorTheme } from "@/custom-theme.json"

export const Scroll = ({dataSource}) => {
  return (
    <View>
      
    </View>
  );
}